Workdir for exefs2elf.py
needs the ARM dev GNU toolchain downloaded and in PATH.
[for arm-none-eabi-(gcc,g++,objcopy,ld)]

place the decrypted exheader.bin file here, and rename it to exh.bin
make a folder, name it exefs, and put the code.bin form the extracted exefs in there.

now, run the python script